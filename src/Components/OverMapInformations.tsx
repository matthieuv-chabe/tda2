
export const OverMapInformations = (_props: {map?: google.maps.Map}) => {
    return (<>
{/*     
    <div style={{
        
        
    }}>
        {props.map?.getBounds()?.getCenter().toJSON().lat.toFixed(4)}, {props.map?.getBounds()?.getCenter().toJSON().lng.toFixed(4)}
    </div> */}
    
    </>)
}
